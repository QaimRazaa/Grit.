class AppAnimations {
  // Animation Durations
  static const Duration buttonPressScale = Duration(milliseconds: 150);
  static const Duration buttonPressOpacity = Duration(milliseconds: 150);
  static const Duration toastFadeIn = Duration(milliseconds: 200);
  static const Duration toastVisibleDuration = Duration(milliseconds: 2100);
  static const Duration toastFadeOut = Duration(milliseconds: 200);
  static const Duration totalToastDuration = Duration(milliseconds: 2500);
  static const Duration splashScreenWait = Duration(milliseconds: 2000);
  static const Duration workoutCompleteCheckmark = Duration(milliseconds: 1000);
  static const Duration autoNavigateWorkoutComplete = Duration(milliseconds: 800);

  // Private constructor to prevent instantiation
  AppAnimations._();
}
